import { Typography } from '@mui/material'

import styles from './Loader.module.scss'

const Loader = () => {
  return <Typography component="span" className={styles.loader} />
}

export default Loader
